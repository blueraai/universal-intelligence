from src.community.tools.__utils__.test import run_all_tests

from src.community.tools.simple_printer.tool import UniversalTool

# Run all tests with default configuration
run_all_tests(UniversalTool)
